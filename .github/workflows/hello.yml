name: Say hello
on:
  push:
    branches:
      - main
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo message
        run: echo "Hello World"
      - name: Give me today's date
        run: date
      - run: ls -l